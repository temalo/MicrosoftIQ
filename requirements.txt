# The data + knowledge generator uses only the Python standard library.
# This file only installs python-pptx for the pitch deck (slides/).
# Optional direct RTI streaming: rti/requirements-eventhouse.txt.
python-pptx>=0.6.21
